{
  "name": "legallink",
  "private": true,
  "version": "0.1.0",
  "description": "LegalLink — orchestration backend (Docker) + frontend (Vite)",
  "scripts": {
    "backend": "docker compose up",
    "backend:build": "docker compose up --build",
    "frontend": "npm --prefix frontend run dev",
    "dev:all": "concurrently -n backend,frontend -c blue,green -k \"npm run backend\" \"npm run frontend\"",
    "dev:all:build": "concurrently -n backend,frontend -c blue,green -k \"npm run backend:build\" \"npm run frontend\"",
    "stop": "docker compose down",
    "install:all": "npm install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
